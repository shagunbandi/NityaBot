# Cloudflare API Token
# Create at: https://dash.cloudflare.com/profile/api-tokens
# Permissions needed: Zone.Zone (Read) + Zone.DNS (Edit)
CF_API_TOKEN=E-XLfrdRCB9k4Gml4lcJqm5OWbyLjdTLa5pkjPz5

# Your base domain (apps deploy as subdomains: app-name.yourdomain.com)
# CNAME records will point subdomains to this domain
# Zone ID is looked up automatically from this domain
CF_BASE_DOMAIN=pocketfusion.in

# Allowed zone suffix for domain validation (prevents deploying to arbitrary domains)
# Usually the same as CF_BASE_DOMAIN
ALLOWED_ZONE_SUFFIX=pocketfusion.in

# Docker network name for Traefik integration
# This must match the external network in your Traefik docker-compose.yml
DOCKER_NETWORK=openclaw_network

# Traefik ACME cert resolver name (from your Traefik config)
# Common values: letsencrypt, cloudflare, default
TRAEFIK_CERTRESOLVER=cloudflare
